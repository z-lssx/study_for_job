# 当前实施状态

更新时间：2026-08-02

## 当前阶段

阶段一·数据底座与投递闭环已完成，M001 阶段总管交接文档已生成。当前等待用户使用稳定启动提示词启动项目唯一大总管；大总管登记宏观控制状态后创建唯一活跃的阶段二总管。

## 已完成

- 产品定位、模块边界和 MVP 范围已经过讨论并写入文档。
- 定时任务、投递信号、准备评估、模拟面试、管理端和导出范围的歧义已经收敛。
- 技术架构方向已经形成，包括 PostgreSQL、前后端分离模块化单体、后台 Worker、混合检索和受限 Agent 场景。
- 文档已按产品、决策、架构和实施状态分层。
- 已升级为“唯一项目大总管 → 当前阶段总管 → 开发 Agent”三层协作；同一时间最多一个活跃大总管对话和一个活跃阶段总管。
- 已建立阶段路线图与任务队列；第一条纵向链路选择为“本地运行骨架 + 目标岗位/投递记录最小闭环”。
- T001 已通过 Codex 桌面任务工具创建，Thread ID 为 `019fbda1-2385-7a01-a603-873ba3fbe188`。
- T001 已扩展为开发库 `study_for_job_dev` 与使用库 `study_for_job` 双数据库方案：共用迁移、隔离业务数据，只有开发库自动加载可重复执行的样例数据。
- 投递工作台已完成“编辑部排期墙”方向的视觉重构，桌面端与 390px 移动端均完成人工交互验证；详情侧栏、阶段即时流转、新增/编辑表单与筛选保持真实 API 数据流。
- T001 已验证 React/Vite、FastAPI/SQLAlchemy、PostgreSQL、迁移和 Docker Compose 的本地运行边界；开发库 `study_for_job_dev` 与使用库 `study_for_job` 数据互不污染。
- 已确认桌面 Web 是唯一正式支持端，后续不再开发移动端专属能力；普通业务页面默认不调用 ImageGen。
- 从 T002 起采用 `task-workspaces/TXXX/` 保存计划、实施/审查过程和验证交接文档。
- 用户终止 T002，并确认小规模开发不需要固定追加独立审查；后续默认相信开发 Agent 的结构化摘要和自验证结果，优先连续推进开发任务。
- T003 已建立自有 AI Gateway/Provider 边界，接入确定性 fake 与环境配置的 DeepSeek OpenAI-compatible provider；切换 provider 不需要修改业务调用方。
- T003 已新增关键 prompt 场景/模板与调用账本迁移，并通过启动迁移器覆盖已有数据卷；开发库保留 T001 数据，使用库没有样例业务事实或调用日志。
- T003 桌面 AI 管理页已支持三个代码批准场景的模板/参数编辑、固定成功/失败诊断、按模块/场景 token 聚合和最近 trace；没有扩展为聊天或供应商平台。
- T003 本地 fake 最终验证为 3 次成功、3 次主动失败诊断、168 个模拟 token；这些失败用于验证错误日志而非线上异常。SophNet base URL 与 `SOPHNET_API_KEY` 兜底已接入并在容器内确认可用，真实 DeepSeek 因模型标识尚未配置而未调用。
- T003 SophNet 收尾增量已由原任务 Agent 完成范围与敏感信息检查，提交 `54af16b` 已推送到 `origin/main`；提交后工作树干净。
- 已建立第一任总管阶段规划；后续每任总管在派发子任务前均须明确本任事项、边界、完成条件和交接条件。
- T004 已建立 PostgreSQL `jobs`/`job_attempts` 队列事实、独立 Worker、处理器注册、`FOR UPDATE SKIP LOCKED` 并发领取、lease token/心跳/过期恢复、有界失败退避和受限任务管理 API。
- T004 固定诊断已验证立即成功、永久失败、失败后成功、最大尝试终止、手动重试、幂等参数漂移和非法自由字段拒绝；未知类型、非法 payload 与未映射异常均安全终止且不回显原始输入。
- T004 真实 PostgreSQL 两线程领取同一任务只产生一个租约；过期租约可由第二 Worker 恢复，旧 token 迟到写回被拒绝。常规 21 项测试与显式 PostgreSQL 4 项集成测试通过。
- 开发库与 usage 库均应用 `003_job_queue.sql`；usage 环境的目标画像、投递、AI 调用、任务和 attempt 均为空，Worker 与 API 同时连接 `study_for_job`。验证后已切回 development，开发库保留 4 条固定诊断任务和 7 条 attempt 供交接复核。
- T004 提交 `16ea6c0dd82fabb1aea37a2dfdf143aee8285386` 已推送到 `origin/main`，任务 Agent 确认提交后工作树干净。
- M001 已完成阶段一任务规划中的全部事项，并生成正式阶段交接文档；当前无进行中子任务。
- 项目大总管唯一性规则、宏观控制状态和稳定启动/恢复提示词已建立；唯一大总管尚未启动。

## 已确认但尚未实现

- 大模型服务使用 DeepSeek V4；OpenAI-compatible 适配边界已经实现，具体可用模型标识、远程 token 口径和错误体仍待真实凭据验证。
- MVP 只要求本地运行，同时避免锁死未来部署扩展。
- embedding 方案暂不锁定。
- 面经抓取只考虑公开内容，并保留用户手动提交正文的降级入口。
- embedding 供应方式仍需在实际检索任务中按需要决定。

## 当前阻塞

暂无功能性阻塞。真实 DeepSeek 调用仍是可选外部验证，不影响下一阶段先推进面经公开来源/手动正文入库。

## 状态维护规则

- 只记录已确认事实，不把建议写成完成状态。
- 每次子任务结束后增量更新，不重写无关历史。
- 详细任务放入 `tasks/`，本文件只保留全局摘要。
