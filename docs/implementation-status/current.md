# 当前实施状态

更新时间：2026-08-02

## 当前阶段与控制状态

- 当前阶段：阶段二“面试情报闭环”，进行中。
- 唯一活跃阶段总管：M002；Thread ID `019fc0db-6ec7-7e72-a3bb-932bb078c328`。
- 来源项目大总管：Thread ID `019fc0d0-5d8a-7e80-b5c5-93de2eb2bd53`。
- 唯一宏观控制事实源为 `program-status.md`；该文件已确认阶段一验收完成、M002 唯一性和阶段二进入条件。
- 当前阶段计划：`management-plans/M002-phase-two.md`。

## 进入条件与已完成基础

- 阶段一“数据底座与投递闭环”已验收，正式交接见 `handoffs/M001-phase-one-handoff.md`。
- 本地 React/FastAPI/PostgreSQL/Compose 骨架、目标画像、投递记录和 development/usage 双数据库隔离可用。
- AI Gateway、关键 Prompt 白名单、Token/Trace 账本已建立；真实 DeepSeek/SophNet 模型标识尚未受控验证，不阻塞先建立原文与状态链路。
- PostgreSQL `jobs`/`job_attempts`、独立 Worker、`FOR UPDATE SKIP LOCKED`、lease token、心跳、过期恢复和有界退避已建立；执行语义为至少一次。

## 当前目标

将公开且无需登录的面经来源、用户手动提交的链接或正文，转成可追溯、可检索、可统计的面试需求情报：

- 原始内容、清洗正文、来源和处理状态持久化。
- URL/内容幂等、失败分类、脱敏原因和补正文降级。
- 原文标注、轮次/内容块/chunk、结构化题目、追问和证据回链。
- 规范题、原始出现记录、不可污染的频率统计和人工修正边界。
- 结构化过滤、词法/相似检索以及由真实质量需求决定的语义召回。

## 当前任务

- T005：面经原始事实与可恢复入库链路，待验证；开发 Thread ID `019fc0e8-de74-72b0-827c-3218a8f51f3e`，提交 `85d21b800bac36fc1ff5037763d4a5b94260ccc0` 已推送。
- T005 已返回完整结构化交接和风险相称验证；因 SSRF、来源许可/robots、原始 HTML 与至少一次 Worker 幂等触发独立安全/数据一致性审查，审查结论前不派发 T006。
- 后续顺序：T006 原文标注与结构化证据链 → T007 规范题与频率事实 → T008 混合检索与情报质量收口。
- 开发任务队列、依赖和状态以 `queue.md` 为准。

## 当前风险与边界

- Worker 为至少一次语义，阶段二处理器必须用业务唯一事实保证重放幂等。
- 抓取只限公开、无需登录、许可边界明确的内容；不绕过限制，始终保留手动正文入口。
- 网络、解析、来源限制和处理错误需要稳定的可重试分类与脱敏原因。
- 用户修订不得改写原始出现事实或污染高频计数。
- embedding 供应方式只在真实检索数据证明需求后决定；不提前引入复杂近似索引。
- 不进入准备轨道、策略建议、导出、多轮 Agent、定时任务或无关队列扩张。

## 下一步

M002 派发并消费 T005 独立安全/数据一致性审查；无阻塞发现时验收 T005 并顺序派发 T006，有阻塞发现时续发原 T005 修复。阶段未完成文档收口和正式交接前不创建下一阶段总管。
