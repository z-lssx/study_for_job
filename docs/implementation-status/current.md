# 当前实施状态

更新时间：2026-08-01

## 当前阶段

开发前交接准备。项目尚未进入代码实现阶段。

## 已完成

- 产品定位、模块边界和 MVP 范围已经过讨论并写入文档。
- 定时任务、投递信号、准备评估、模拟面试、管理端和导出范围的歧义已经收敛。
- 技术架构方向已经形成，包括 PostgreSQL、前后端分离模块化单体、后台 Worker、混合检索和受限 Agent 场景。
- 文档已按产品、决策、架构和实施状态分层。
- 已确定采用“总管 Agent 对话 + 独立开发任务对话”的协作方式；总管接力由用户根据交接文档手动完成。

## 已确认但尚未实现

- 大模型服务使用 DeepSeek V4，具体模型档位和参数后续按场景决定。
- MVP 只要求本地运行，同时避免锁死未来部署扩展。
- embedding 方案暂不锁定。
- 面经抓取只考虑公开内容，并保留用户手动提交正文的降级入口。
- 第一条纵向开发链路由总管 Agent 在形成实施规划时决定。

## 当前阻塞

无。下一步由用户启动新的总管 Agent 对话，由其检查现有文档并建立路线图和首个子任务说明。

## 状态维护规则

- 只记录已确认事实，不把建议写成完成状态。
- 每次子任务结束后增量更新，不重写无关历史。
- 详细任务放入 `tasks/`，本文件只保留全局摘要。
