# T013 实施记录：沉静式桌面工作台与信息层级

更新时间：2026-08-06

## Skill 约束与设计方向

已完整读取 `C:/Users/zengw/.codex/skills/frontend-design/SKILL.md`。本任务选择“克制、精确的沉静桌面工作台”方向：通过低饱和暖灰/森林绿、稳定的宋体标题与无衬线正文、受控留白和细边界建立辨识度；不使用泛紫渐变、高饱和装饰、无意义动效或图片资产。最小化改动现有 React 组件与 CSS，不改变业务 API、状态字段和四轨道事实边界。

## 现状盘点

- 全局壳层使用高饱和 `acid/coral/cobalt`、明显噪点、旋转轨道和重阴影，视觉注意力高于准备资产。
- 顶部 8 个入口等权平铺，桌面中等宽度下拥挤，准备轨道与管理入口没有层级。
- 申请页首屏约 475px，并使用大号标题、轨道装饰和错落列；核心推进数字与下一步进入视野较慢。
- 知识、算法、项目、实习的新建表单常驻左栏；项目/实习内部的证据、版本和情报编辑已使用原生 `details`，可继续复用。
- 情报和 AI 页面首屏高度偏大；已有数据流与页面功能完整，不应重写。

## 实施清单

- [完成] 统一全局色阶、字体层级、按钮、边界、阴影和动效节奏。
- [完成] 将主导航按工作流、四条准备轨道和设置入口分组，保留全部既有入口。
- [完成] 压缩申请页首屏与分区间距，移除纯装饰轨道，核心数字与目标画像优先。
- [完成] 将四轨道的新建动作收进原生折叠，保持列表/状态/证据为页面主体。
- [完成] 压缩情报与 AI 首屏，不改变其 API、台账和管理能力。
- [完成] 执行前端静态语法检查、`git diff --check`，记录未执行运行态验证。

## 完成后的结构

- 顶部壳层将“申请/情报”“知识/算法/项目/实习”“画像/AI”分为三组；全部仍调用原有 `setActiveView` 或目标画像弹窗。
- 申请页取消纯装饰 orbit，将首屏缩短为核心主张、主动作和三项事实指标；目标画像与机会看板紧随其后。
- 知识与算法将新增表单折叠到左侧轨道摘要，卡片/题单成为主内容；知识个人笔记继续按需展开，算法随机练习仍只展示一题。
- 项目与实习将新建证据包折叠；选中资产的基本事实先显示只读摘要，编辑表单按需展开；证据、表达版本、材料和情报关联仍沿用原有分区与 `details`。
- 情报与 AI 页面仅压缩首屏和统一视觉 token，台账、检索、Prompt、用量和诊断结构未调整。
- 中等宽度下主导航保留原入口；窄屏沿用同一导航横向滚动，没有引入移动端专属导航。

## 验证记录

- 通过：对修改的 React/JSX 入口执行 TypeScript `--noEmit --noCheck --allowJs --jsx react-jsx` 静态语法解析。
- 通过：限定 T013 文件执行 `git diff --check`。
- 未执行：`npm run build`（阶段明确禁止）。
- 未执行：页面人工验证；检查时本机 `5173` 端口没有 Vite 监听，按阶段偏好未自行启动/构建服务，也未编写页面访问脚本。
- 未执行：API、数据库/迁移和后端验证；本任务没有修改这些边界。

## 稳定文档判断

本次仅改变既有桌面工作台的视觉层级和信息收纳，不产生新的 API、数据流、状态机、可靠性或工程机制。按 `docs/tech-architecture/AGENTS.md`，不新增或改写技术架构稳定实现文档；长期业务事实继续由四轨道及情报稳定文档承载。

## 明确保持不变

- `/api` 请求、后端、数据库、迁移和四轨道模型均不修改。
- 知识掌握、算法练习、项目事实/版本、实习事实/版本/材料保持独立。
- 情报 occurrence/frequency 继续只作可解释参考，不生成统一评分。
- 随机练习继续一次只返回一题。
- 不实现 semantic recall、embedding/pgvector、RAG、Agent、策略导出或移动端专属导航。
