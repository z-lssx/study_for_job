# T013 任务计划：沉静式桌面工作台与信息层级

## 目标

在 T009–T012 入口稳定后，对现有桌面工作台做一次受控视觉和信息架构优化：颜色/装饰沉静化，首屏与纵向长度合理，核心信息优先，次要内容按需展开。

## 必读与 Skill

除根/产品/决策/架构/状态文档和前序 handoff 外，开始前必须完整读取 `C:/Users/zengw/.codex/skills/frontend-design/SKILL.md`，提示词绑定 `@frontend-design`。

## 范围

复用现有 API、数据流和四轨道边界；通过层级、按钮、折叠、抽屉或弹窗收纳次要信息；保持桌面 Web 正式支持和基础响应式，不扩展移动端专属导航。

## 非范围与验证

不做无界面重写、不改 API/数据模型、不引入 ImageGen、不做额外 Agent/RAG；页面默认人工验证，不写访问脚本，不运行 `npm run build`。完成 `implementation.md`、`verification-and-handoff.md`，记录人工检查与未执行项，提交推送仅限 T013。
