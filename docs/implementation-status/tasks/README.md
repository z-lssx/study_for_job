# 子任务记录

总管 Agent 为每个开发任务创建独立文件，建议命名：`T001-short-title.md`。

任务文件至少包含：

- 目标
- 背景与依赖
- 开始前必须读取的文档和 `AGENTS.md`
- 允许修改的范围
- 明确不做的内容
- 可由开发 Agent 自主决定的内容
- 验收条件
- 验证要求
- 完成后的文档更新要求
- 完成结果或交接说明

一个任务只对应一个主要结果，避免把多个未确定的领域塞进同一对话。
