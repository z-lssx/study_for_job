# 项目宏观控制状态

更新时间：2026-08-08

本文件是唯一项目大总管的持久控制面。它只记录宏观控制权、阶段级结论和跨阶段风险，不复制任务实现细节。

## 大总管控制权

- 逻辑身份：项目唯一大总管。
- 当前状态：活跃。
- 活跃大总管对话：当前用户授权对话；Thread ID `019fc0d0-5d8a-7e80-b5c5-93de2eb2bd53`。
- 恢复检查点：阶段一至四全部 MVP 业务阶段已经验收完成。用户在最终收口确认前新增并确认了 `docs/design/frontend-redesign.md`，要求先完成整版前端 UI 重设计；项目因此不得标记为“项目完成”。用户同时明确授权本轮不设阶段总管，由项目大总管直接创建一个实现对话，并允许该实现对话在内部使用子 Agent 分工。
- 下一项允许动作：提交并推送设计简报与宏观状态，随后创建唯一的前端整版重设计实现对话。该对话调整前端 UI、交互层级和真实前端路由，并可同步补齐有明确页面消费方的必要后端 API；必须先盘点和复用现有接口，不得按组件粒度拆接口或扩张业务范围。完成后返回项目大总管验收。

若本节已经记录“活跃”大总管，新对话在无法确认自己是同一对话或用户授权替换时不得修改状态或创建任务。

## 当前宏观阶段

- 最近验收完成：阶段四“策略闭环、导出与 MVP 收口”。
- 验收证据：`docs/implementation-status/handoffs/M004-phase-four-handoff.md`；阶段交接事实修正提交 `7226474bb7d6e73215e7ce430d5263111099d202` 已推送且工作树干净。
- 当前阶段：阶段五“桌面工作台整版重设计”，准备启动。
- 活跃阶段总管：无（M004 已完成验收并清除控制权）。
- 活跃直接实现对话：无；等待创建。此字段是用户对阶段五“不设阶段总管”的一次性明确授权，不改变其他阶段的默认三层治理规则。

## 宏观阶段表

| 阶段 | 状态 | 进入依据 | 退出结论/下一步 |
| --- | --- | --- | --- |
| 阶段一：数据底座与投递闭环 | 已完成 | 初始项目范围 | 本地骨架、投递闭环、AI Gateway、PostgreSQL Worker 已完成并交接 |
| 阶段二：面试情报闭环 | 已完成 | 阶段一验收完成且启动审计通过 | 正式交接已验收；语义召回延期为后续能力 |
| 阶段三：四条准备轨道 | 已完成 | 阶段二正式交接已验收 | 四轨道、单题练习、证据边界和受控工作台层级优化已交接验收 |
| 阶段四：策略闭环、导出与 MVP 收口 | 已完成 | 阶段三正式交接已验收 | 策略、主动建议、Markdown/JSON 导出和文档收口已交接验收；等待用户确认 MVP 收口 |
| 阶段五：桌面工作台整版重设计 | 准备启动 | 阶段一至四已验收；用户已确认正式设计简报和直接实现授权 | 创建唯一实现对话；完成全部现有 MVP 页面 UI 替换后由项目大总管验收 |
| 阶段六：受限 Agent 场景 | MVP 后 | 用户完成最终收口并决定继续 | 不属于当前前端重设计范围 |

## 已完成基础能力

- 本地 React/FastAPI/PostgreSQL/Compose 运行骨架。
- 目标画像、投递记录和桌面看板。
- development/usage 双数据库隔离。
- AI Gateway、DeepSeek-compatible/SophNet 环境边界、Prompt 配置和 Token/Trace 日志。
- PostgreSQL 任务队列、独立 Worker、幂等领取、租约恢复和失败退避。

详细实现事实由技术架构文档和 M001 阶段交接承载，本文件不重复任务级验证命令。

## 当前任务目标基线

阶段五以 `docs/design/frontend-redesign.md` 为唯一正式设计输入，在不改变现有业务事实和可信边界的前提下，将全部现有 MVP 页面统一重设计为“今日重点首页 + 独立投递页 + 可收起左侧工作台导航”的沉静桌面工作台。深层内容使用真实前端路由，次要信息通过折叠、抽屉、弹窗和短提示渐进披露，四条准备轨道按各自真实任务分别设计。允许为明确页面需求同步补齐必要后端 API，但必须优先复用现有能力，并保持 PostgreSQL 唯一事实源。

用户已明确本轮不设阶段总管，由一个独立实现对话直接完成；该对话可以在内部使用子 Agent，但不得创建新的用户侧开发对话或管理角色。所有视觉实现必须绑定 `frontend-design`。本轮只做逻辑检查，不运行测试或构建；完成后仍由项目大总管验收，且用户确认最终收口前不得标记“项目完成”。

## 跨阶段风险与未决事项

- Worker 是至少一次语义，面经处理器必须实现业务幂等。
- 抓取仅限公开、无需登录内容，并始终保留手动正文入口。
- embedding 方案延后到检索需求明确时决定。
- 真实 DeepSeek/SophNet 模型标识和远程调用仍未验证，但不阻塞原文与状态链路。
- 用户已确认同义/语义召回延期；embedding/pgvector 与真实语义质量评估不阻塞阶段二或阶段三主要功能开发。
- 用户最新最高优先级规则要求后续编码不安排测试相关内容，编码完成后只做逻辑检查；未执行迁移、API、数据库、页面和构建验证仍须如实记录，不得伪造通过结论。
- 阶段四的策略必须规则优先、证据可解释，不读取全量正文制造成本或幻觉，不把情报频率、投递阶段或 AI 草稿误写成用户事实或统一评分。
- 来源许可、真实模型验证和 embedding 供应方式仍属于后续阶段内需受边界约束的实现事项。
- MVP 的准备评估、任务建议、Markdown/JSON 导出和跨模块验收统一进入阶段四，避免在四条轨道尚未稳定时提前整合。
- 多轮 Agent 必须继续位于 MVP 后，不能借随机抽题或准备评估扩入 MVP。
- 本轮前端重设计不得复制情报证据、改变用户事实或增加第二事实源。可按设计简报先盘点后补齐必要后端 API；不得按 React 组件粒度拆接口，不得借 UI 调整扩张业务能力。若必须修改数据库结构，需在实现交接中单独说明理由、影响与未验证边界。
- 页面替换不保留旧版兼容、双版本、功能开关或迁移阶段；但必须保持现有 MVP 功能和数据流边界。

## 状态维护协议

- 大总管状态只使用：`未启动 | 活跃 | 暂停 | 项目完成`。
- 宏观阶段状态只使用：`未开始 | 准备启动 | 进行中 | 待验收 | 已完成 | 阻塞 | MVP 后`。
- 同一时间最多记录一个活跃阶段总管；创建成功后必须写入 Thread/session ID。
- 阶段未验收前不得清除当前阶段或创建下一阶段总管。
- 阶段验收后记录交接文档、提交证据和剩余风险，再更新路线图。
- 上下文或事实冲突影响判断时写入“暂停”和恢复检查点，不创建第二个大总管逻辑身份。
- 不把启动提示词、对话记忆或未验证计划写成完成事实。
- 阶段五经用户明确授权采用一次性直接实现对话；该例外只适用于本轮纯前端 UI 调整，不推广到后续宏观阶段。
