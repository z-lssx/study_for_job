# 项目宏观状态

更新时间：2026-08-02

## 当前总览

- 阶段一“数据底座与投递闭环”：已完成。
- 当前没有进行中的阶段总管或开发任务。
- 下一阶段：阶段二“面试情报闭环”。
- 下一入口：由项目大总管创建阶段二总管；阶段二总管先建立自己的阶段计划，再从 T005 开始拆分开发任务。

## 已完成基础能力

- 本地 React/FastAPI/PostgreSQL/Compose 运行骨架。
- 目标画像、投递记录和桌面看板。
- 开发库/使用库隔离。
- AI Gateway、DeepSeek-compatible/SophNet 环境边界、Prompt 配置和 Token/Trace 日志。
- PostgreSQL 任务队列、独立 Worker、幂等、租约恢复和失败退避。

## 下一阶段宏观目标

建立可追溯的面试情报输入层：公开来源或手动正文进入系统，完成原文持久化、URL/内容幂等、失败状态与补正文降级，再逐步形成结构化题目、证据回链、检索和频率统计。

## 跨阶段风险

- Worker 是至少一次语义，面经处理器必须实现业务幂等。
- 抓取仅限公开、无需登录内容，并始终保留手动正文入口。
- embedding 方案继续延后到检索需求明确时决定。
- 真实 DeepSeek/SophNet 模型标识和远程调用仍未验证，但不阻塞先完成原文与状态链路。
- 阶段三开始前必须验证情报数据质量，避免准备轨道建立在不可追溯或低质量抽取上。

## 宏观阶段基线

现有 `roadmap.md` 是初始基线。第一任项目大总管启动后应从 MVP 完整性角度复核阶段划分，特别确认数据导出、阶段整合与 MVP 收口是否需要独立宏观阶段；多轮模拟面试等 Agent 场景必须继续标记为 MVP 后内容。
